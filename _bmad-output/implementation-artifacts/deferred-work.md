# Travail différé

## Deferred from: code review of 1-6-accroches-francaises (2026-08-28)

- **État de module de `enrich/llm.py` scopé « par processus », pas « par run ».** `_env_charge`, `_avertissement_cle_absente_emis` et `_avertissement_echec_api_emis` sont des variables globales du module, réinitialisées uniquement par la fixture `autouse` de `tests/test_llm.py`. Sans conséquence aujourd'hui : aucun autre module n'appelle encore `enrich.llm` (non branché dans `collect.py`, cf. « Hors périmètre » de la Story 1.6). À revoir si le module est un jour encapsulé dans un objet de run par `pipeline.py` (**Story 1.8**) — un run nocturne et le suivant ne devraient pas partager cet état en mémoire dans un processus long-vivant. (`src/veille/enrich/llm.py`)

## Deferred from: code review of 1-5-quotas-par-section (2026-08-28)

- **`pour_le_metier` est structurellement toujours vide dans le socle réel.** `config/sources.yaml` déclare 4 sources, aucune en registre `pour_le_metier` (vérifié : `grep registre: config/sources.yaml` — seulement `apprendre` et `ce_qui_bouge`). Le mécanisme de quotas fonctionne correctement sur les deux registres peuplés, mais la 3ᵉ section du digest (« Pour le métier ») ne recevra jamais d'entrée tant qu'aucune source de ce registre n'existe — silencieux, car `RapportQuotas.resume()` n'a rien à rapporter pour un registre qui ne reçoit jamais d'item (différent d'AC6, qui couvre un registre *inconnu de la config de quotas*, pas un registre *jamais peuplé par le socle*). À traiter en **Epic 2** (élargissement du socle à 15-20 sources) : une source emploi/carrière comblerait naturellement ce registre.
- **`rapport_quotas`/`rapport_classement` sur-comptent si le même objet `Item` apparaît plusieurs fois dans leur liste d'entrée** (diffing par `id()` plutôt que par position). Non atteignable via `collecter()` — `dedup.py` garantit qu'un `Item` gagnant n'apparaît qu'une fois — mais précondition non documentée avant la revue de la Story 1.5. Reporté : un redesign (diffing par position) est prématuré pour un cas non exploitable aujourd'hui ; la précondition est désormais documentée dans les deux docstrings (`src/veille/filter.py`).

## Deferred from: code review of 1-4-scoring-par-profil (2026-08-28)

- **AD-1 : `collect.py` orchestre trois étapes du pipeline, `pipeline.py` n'existe toujours pas.** L'architecture réserve l'ordonnancement à un orchestrateur dédié (Structural Seed), mais `collect.py` enchaîne aujourd'hui collecte → dédoublonnage → seuil de signal → scoring. L'écart existait déjà pour `dedup` (Story 1.3) et la Task 5 de la Story 1.4 demandait explicitement ce branchement — c'est une contradiction spec ↔ architecture, pas une désobéissance. Reporté à la **Story 1.8** : l'orchestrateur prend son sens quand les étapes rendu et publication existent ; l'extraire maintenant pour deux appels serait prématuré. (`src/veille/collect.py:22-32`, `200-209`)
- **`Score.motifs` est calculé puis intégralement jeté.** La docstring justifie le champ par l'explicabilité (« Abdoulaye doit pouvoir comprendre pourquoi un item est remonté »), mais `rapport_classement` ne conserve que `score.valeur` et `collecter()` détruit les `ItemScore` — aucun appelant ne peut atteindre un seul motif. Reporté plutôt que corrigé : il n'existe aucun consommateur avant le rendu HTML (**Story 1.8**), et exposer le champ maintenant serait de la généralité spéculative. À reprendre en même temps que l'affichage d'une entrée. (`src/veille/filter.py:143`, `src/veille/collect.py:208`)
- **Plus aucun tri par fraîcheur.** `classer` trie exclusivement par score et détruit l'ordre issu de la collecte. Les stories précédentes instrumentaient pourtant `nb_dates_approximatives` parce que « le tri par fraîcheur remonte le mauvais ». L'ordonnancement final du digest relève des quotas par section — **Story 1.5** — où la question « à score égal, quoi en premier ? » se pose de toute façon. (`src/veille/filter.py:204`)

## Deferred from: code review of 1-1-collecte-premiere-source (2026-07-28)

- **Collision de `guid` de repli sur `source_id + titre`.** Deux entrées sans lien ni id partageant un titre générique (ex. "Weekly Update") produisent le même hash et collapsent silencieusement. Sans conséquence tant qu'aucune logique de dédoublonnage n'existe (Story 1.1) — devient consequential dès la **Story 1.3** (dédoublonnage), à traiter à ce moment-là.
- **Aucun timeout réseau sur `feedparser.parse()`.** Une source qui ne répond jamais bloquerait indéfiniment tout le run. Relève explicitement d'**Epic 2** (Story 2.2 tolérance aux pannes, Story 2.3 sources sensibles au débit).
- **Pas de distinction entre échec HTTP (404/500) et flux malformé.** Tout finit dans le même message générique "flux malformé", rendant le diagnostic pénible. Recoupe directement **Epic 4** (FR-12/FR-13, détection et signalement des sources défaillantes) — à intégrer à ce moment plutôt qu'en scope creep maintenant.
- **`models.py` ne valide que `date_publication`.** `titre`, `url`, `guid`, `source_id`, `contenu_brut` peuvent être des chaînes vides sans que `Item` s'en plaigne. L'AC2 actuel n'exige que la présence des champs, pas leur non-vacuité. À muscler quand la **Story 1.8** (rendu HTML) rendra les champs vides visiblement cassés à l'écran.
- **Aucun User-Agent explicite envoyé par `feedparser`.** Certains serveurs bloquent ou limitent le user-agent par défaut des clients Python. Risque spéculatif, non observé sur le flux OpenAI testé. À surveiller à mesure que le socle s'élargit (**Epic 2**).
- **Pas de requête conditionnelle (ETag/Last-Modified).** Chaque run retélécharge l'intégralité de chaque flux. Optimisation pertinente seulement à l'échelle du socle complet tournant nuit après nuit (**Epic 2/3**).
- **`contenu_brut` est du contenu brut non échappé (HTML potentiel du flux source).** Risque XSS si affiché tel quel — mais aucun rendu HTML n'existe encore dans le pipeline. À vérifier explicitement à la **Story 1.8** : s'assurer que l'échappement automatique de Jinja2 reste actif sur ce champ, ne jamais utiliser le filtre `|safe` dessus.
